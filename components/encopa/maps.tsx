export function MapLinks({address}:{address:string}){if(!address.trim())return <p className="text-sm text-[#687370]">住所を入力すると地図を開けます。</p>;const q=encodeURIComponent(address.trim());return <div className="flex flex-wrap gap-2"><a className="enc-secondary" href={`https://www.google.com/maps/search/?api=1&query=${q}`} target="_blank" rel="noopener noreferrer">Googleマップ ↗</a><a className="enc-secondary" href={`https://maps.apple.com/?q=${q}`} target="_blank" rel="noopener noreferrer">Appleマップ ↗</a></div>}
